<main>
    <ul>
        {{#each items}}
        <li>
            <h3>{{this.name}}</h3>
            <img src={{this.images.0.url}} alt={{this.name}}>
            <a href="/tracks/{{this.id}}">View tracks</a>
        </li>
        {{/each}}
    </ul>
</main>